pipeline {
properties([parameters([choice(choices: ['Env1', 'Env2'], description: 'Current Environnement', name: 'ENV')])])
  
  agent any
  stages {
    stage('Build') {
      steps {
        echo "building"
        
      }
    }

  }
}
