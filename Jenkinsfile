pipeline {
properties([parameters([string(defaultValue: 'JOE', name: 'NOM', trim: true)])])
  
  agent any
  stages {
    stage('Build') {
      steps {
        echo "building"
        
      }
    }

  }
}
