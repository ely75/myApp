pipeline {
properties([
        parameters([
            choice(choices: ['ONE', 'TWO'], description: "Current Envoronnement", name: "ENV"),
        ])
    ])
  
  agent any
  stages {
    stage('Build') {
      steps {
        echo "building"
        
      }
    }

  }
}
