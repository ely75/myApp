pipeline {
properties([parameters([choice(choices: 'test\ndeploy', name: 'baranch')])])
  
  agent any
  stages {
    stage('Build') {
      steps {
        echo "building"
        
      }
    }

  }
}
